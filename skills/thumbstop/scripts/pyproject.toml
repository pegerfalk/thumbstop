[project]
name = "thumbstop"
version = "0.1.0"
description = "Music cue analysis for the /thumbstop skill (inherited from /brag)."
requires-python = ">=3.11"
dependencies = [
  "librosa>=0.10.2",
  "numpy>=1.26",
  "scipy>=1.11",
  "soundfile>=0.12",
]

[tool.uv]
package = false
